# Environment variables for the portfolio site
# Copy to .env and fill in values

# Path to the Obsidian vault (absolute or relative to project root)
# Default: ../MyHub (sibling directory convention)
VAULT_PATH=../MyHub

# Future: newsletter API key, analytics, etc.
# NEWSLETTER_API_KEY=
